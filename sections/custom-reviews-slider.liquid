<!-- customer-reviews.liquid -->
<section class="customer-reviews">
  <div class="reviews-container">
    <!-- Título de la sección -->
    <h2>{{ section.settings.section_title }}</h2>

    <div class="reviews-slider">
      <!-- Inicio del loop de reseñas -->
      {% for block in section.blocks %}
        <div class="review-item">
          <!-- Estrellas de reseña -->
          <div class="review-rating">
            {% for i in (1..block.settings.review_rating) %}
              &#9733;
            {% endfor %}
          </div>

          <!-- Texto de la reseña -->
          <p class="review-text">{{ block.settings.review_text }}</p>

          <!-- Nombre del cliente -->
          <p class="review-author">{{ block.settings.review_author }}</p>

          <!-- Imagen del cliente -->
          <div class="review-image">
            <img
              src="{{ block.settings.image | image_url: width: 30, height: 30 }}"
              alt="{{ block.settings.review_author}}"
            >
          </div>
        </div>
      {% endfor %}
      <!-- Fin del loop de reseñas -->
    </div>
  </div>
</section>

<!-- Estilos (puedes mover esto a tu archivo CSS o usar la hoja de estilo que prefieras) -->
<style>
  .customer-reviews {
    padding: 20px;
    background-color: #f9f9f9;
  }
  .reviews-container {
    max-width: 1200px;
    margin: 0 auto;
    text-align: center;
  }
  .reviews-slider {
    display: flex;
    overflow-x: auto;
    scroll-behavior: smooth;
  }
  .review-item {
    margin: 20px;
    padding: 20px;
    border: 1px solid #ddd;
    border-radius: 8px;
    background-color: white;
    flex: 0 0 300px;
    text-align: left;
  }
  .review-rating {
    font-size: 20px;
    color: #ffcc00;
    margin-bottom: 10px;
  }
  .review-text {
    font-size: 16px;
    margin-bottom: 10px;
  }
  .review-author {
    font-size: 14px;
    font-weight: bold;
    margin-bottom: 10px;
  }
  .review-image img {
    border-radius: 50%;
  }
</style>

{% schema %}
  {
  "name": "Customer Reviews",
  "settings": [
    {
      "type": "text",
      "id": "section_title",
      "label": "Section Title",
      "default": "Why did more than 20,000 happy users choose Fresh Juice Blender?"
    }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Review",
      "settings": [
        {
          "type": "textarea",
          "id": "review_text",
          "label": "Review Text",
          "default": "This blender is perfect! It not only blends frozen fruit but it crushes ice so well too. It's easy to clean and fits in the cup holder in my car."
        },
        {
          "type": "text",
          "id": "review_author",
          "label": "Author Name",
          "default": "Jenny M."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Review Image"
        },
        {
          "type": "range",
          "id": "review_rating",
          "label": "Rating (1-5)",
          "min": 1,
          "max": 5,
          "default": 5
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Customer Reviews",
      "category": "Custom"
    }
  ]
}
{% endschema %}
